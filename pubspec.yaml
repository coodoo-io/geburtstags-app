name: geburtstags_app
description: App für Geburtstagerinnerungen
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ^3.16.8

dependencies:
  clock: ^1.1.1
  flutter:
    sdk: flutter

  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  test: ^1.24.3
flutter:
  uses-material-design: true

  assets:
    - assets/images/
