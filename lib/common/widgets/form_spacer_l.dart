import 'package:flutter/material.dart';

class FormSpacerL extends StatelessWidget {
  const FormSpacerL({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20);
  }
}
